#include <bits/stdc++.h>
using namespace std;

int main(){
    // vector<string> ans = {"flower","flow","flight"};
    // sort(begin(ans), end(ans));
    // for(auto str : ans){
    //     cout << str << endl;
    // }
    int t = 2;
    while(t--){
        char c;
        cin >> c;
        int k ;
        cin >> k;
        cout << c << " " << k << endl;
    }
    return 0;
}